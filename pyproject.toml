[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-sim-bridge"
version = "0.1.0"
description = "Simulation bridge — connect sim environments to PLATO"
license = {text = "MIT"}
requires-python = ">=3.8"
